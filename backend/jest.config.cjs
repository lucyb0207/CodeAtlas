module.exports = {
  testEnvironment: "node",

  transform: {},

  testPathIgnorePatterns: [
    "/node_modules/",
    "/tmp/",
    "/tmp_test/",
  ],

  watchPathIgnorePatterns: [
    "/tmp/",
  ],
};